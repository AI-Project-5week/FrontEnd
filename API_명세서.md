# AI 가계부 백엔드 API 명세서

작성일: 2026-09-23

## 개요

프론트(Streamlit)는 아래 10개 API만 호출합니다. 경로와 JSON 키 이름을 이 문서대로 맞추면 프론트 수정 없이 바로 연결됩니다.

**공통 규칙**

- 기본 주소: `http://localhost:8000` (다르면 프론트 실행 시 환경변수 `API_URL`로 지정)
- 요청·응답은 모두 JSON, 문자 인코딩 UTF-8
- 날짜는 `YYYY-MM-DD` (예: `2026-09-22`), 월은 `YYYY-MM` (예: `2026-09`)
- 금액은 원 단위 정수 (예: `5800`)
- 실패 시 4xx/5xx 상태 코드를 반환하면 프론트가 "서버 오류가 발생했어요 (코드)"로 표시
- AI 응답이 느릴 수 있어 프론트 타임아웃은 120초

| 메서드 | 경로 | 용도 | 사용 화면 |
| --- | --- | --- | --- |
| GET | `/categories` | 카테고리 목록 | 내역 입력, 내역 목록 |
| GET | `/budget` | 월 예산 조회 | 대시보드 |
| PUT | `/budget` | 월 예산 저장 | 대시보드 |
| GET | `/expenses` | 지출 내역 목록 | 내역 목록 |
| POST | `/expenses` | 지출 내역 추가 | 내역 입력 |
| DELETE | `/expenses/{id}` | 지출 내역 삭제 | 내역 목록 |
| GET | `/stats` | 월 통계 | 대시보드 |
| POST | `/parse` | 자연어 문장 → 지출 항목 (AI) | 내역 입력 |
| POST | `/chat` | 소비 상담 채팅 (AI) | AI 상담 |
| GET | `/report` | 월간 소비 리포트 (AI) | AI 상담 |

## 기본 정보 API

### GET /categories

카테고리 목록을 문자열 배열로 반환합니다. 프론트는 이 순서 그대로 선택지와 필터를 만듭니다.

```json
["식비", "카페", "교통", "쇼핑", "생활", "문화", "의료", "기타"]
```

### GET /budget

해당 월 예산을 반환합니다. 설정된 예산이 없으면 `amount`를 `0`으로 주세요 (404 아님).

| 파라미터 | 위치 | 필수 | 예시 |
| --- | --- | --- | --- |
| `month` | 쿼리 | O | `2026-09` |

```json
{"month": "2026-09", "amount": 500000}
```

### PUT /budget

해당 월 예산을 저장합니다. 이미 있으면 덮어씁니다. 응답은 저장된 값을 그대로 돌려주면 됩니다.

```json
{"month": "2026-09", "amount": 500000}
```

## 지출 내역 API

지출 항목 하나는 아래 필드로 구성됩니다. `id`는 삭제에 쓰이므로 목록 응답에 반드시 포함해야 합니다.

| 필드 | 타입 | 설명 | 예시 |
| --- | --- | --- | --- |
| `id` | int | 내역 ID (서버 생성) | `1` |
| `date` | string | 지출 날짜 `YYYY-MM-DD` | `2026-09-22` |
| `item` | string | 내용 (가게·품목) | `스타벅스` |
| `amount` | int | 금액 (원) | `5800` |
| `category` | string | `/categories` 중 하나 | `카페` |
| `memo` | string | 메모, 없으면 `""` | `""` |

### GET /expenses

해당 월 내역을 날짜 최신순으로 반환합니다. 내역이 없으면 빈 배열 `[]`을 주세요.

| 파라미터 | 위치 | 필수 | 예시 |
| --- | --- | --- | --- |
| `month` | 쿼리 | O | `2026-09` |
| `category` | 쿼리 | X (없으면 전체) | `카페` |

```json
[
  {"id": 1, "date": "2026-09-22", "item": "스타벅스", "amount": 5800, "category": "카페", "memo": ""},
  {"id": 2, "date": "2026-09-20", "item": "점심", "amount": 9000, "category": "식비", "memo": "회사"}
]
```

### POST /expenses

내역을 추가합니다. 요청에는 `id`를 뺀 5개 필드가 오고, 응답은 `id`를 포함한 저장 결과입니다.

```json
{"date": "2026-09-22", "item": "스타벅스", "amount": 5800, "category": "카페", "memo": ""}
```

### DELETE /expenses/{id}

내역을 삭제합니다. 성공 시 200 + 아무 JSON(예: `{"ok": true}`), 없는 ID면 404.

### GET /stats

대시보드 카드와 차트에 쓰는 월 통계입니다. `by_category`는 금액 큰 순, `by_day`는 날짜 순으로 주세요. 내역이 없으면 `total`·`count`는 `0`, 나머지는 `{}`입니다.

| 파라미터 | 위치 | 필수 | 예시 |
| --- | --- | --- | --- |
| `month` | 쿼리 | O | `2026-09` |

```json
{
  "total": 14800,
  "count": 2,
  "by_category": {"식비": 9000, "카페": 5800},
  "by_day": {"2026-09-20": 9000, "2026-09-22": 5800}
}
```

## AI API

AI 기능은 백엔드가 로컬 Ollama(`exaone3.5:2.4b`)를 호출해 처리합니다. 프론트는 텍스트만 주고받으며, 금액 합계 같은 계산은 백엔드가 DB에서 직접 해서 프롬프트에 넣는 것을 권장합니다 (작은 모델은 계산을 자주 틀림).

### POST /parse

말하듯 쓴 문장을 지출 항목으로 바꿉니다. **저장은 하지 않습니다.** 프론트가 결과를 입력 칸에 채우고, 사용자가 확인한 뒤 `POST /expenses`로 저장합니다.

```json
요청: {"text": "어제 스벅 5800원"}
응답: {"date": "2026-09-22", "item": "스타벅스", "amount": 5800, "category": "카페"}
```

- `date`를 못 정하면 오늘 날짜, `amount`를 못 찾으면 `0`
- `category`는 반드시 `/categories` 값 중 하나 (모르면 `기타`)

### POST /chat

`month`에 해당하는 달의 소비 내역을 바탕으로 질문에 답합니다. `month`는 사용자가 화면에서 선택한 월이며 항상 전달됩니다. `history`는 이전 대화를 오래된 순서로 담고 있으며, 첫 질문이면 `[]`입니다.

| 필드 | 타입 | 필수 | 예시 |
| --- | --- | --- | --- |
| `message` | string | O | `카페에 얼마 썼어?` |
| `month` | string | O | `2026-09` |
| `history` | array | O (첫 질문이면 `[]`) | 아래 참고 |

```json
요청: {
  "message": "카페에 얼마 썼어?",
  "month": "2026-09",
  "history": [
    {"role": "user", "content": "안녕"},
    {"role": "assistant", "content": "안녕하세요!"}
  ]
}
응답: {"answer": "이번 달 카페에는 14,100원을 쓰셨어요."}
```

### GET /report

해당 월 소비 리포트를 만듭니다. `report`는 마크다운으로 화면에 그대로 렌더링됩니다 (제목 `###`, 목록 `-`, 굵게 `**` 사용 가능).

| 파라미터 | 위치 | 필수 | 예시 |
| --- | --- | --- | --- |
| `month` | 쿼리 | O | `2026-09` |

```json
{"report": "### 요약\n이번 달 총지출은 185,000원으로..."}
```

## 프론트 연결 방법

프론트의 API 호출은 전부 `가계부/api_client.py`에 모여 있습니다. 백엔드 주소가 `http://localhost:8000`이 아니면 프론트를 이렇게 실행합니다.

```
set API_URL=http://192.168.0.10:8000
python -m streamlit run app.py
```

**연결 전 체크리스트**

- [ ] 포트 8000에서 서버 실행 (예: `uvicorn main:app --port 8000`)
- [ ] 경로와 JSON 키 이름이 이 문서와 일치
- [ ] `GET /expenses` 응답에 `id` 포함
- [ ] 데이터가 없을 때 404 대신 `[]`, `0`, `{}` 반환
- [ ] `/parse`의 `category`가 `/categories` 값 중 하나
- [ ] AI 응답 120초 이내
- [ ] 다른 PC에서 접속한다면 `--host 0.0.0.0`으로 실행
